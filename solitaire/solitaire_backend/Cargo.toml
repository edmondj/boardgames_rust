[package]
name = "solitaire_backend"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
boards = { path = "../../boards" }
regex = "1"
lazy_static = "1"
async-trait = "0"